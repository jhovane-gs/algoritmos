algoritmo "semnome"
var
   cont, n, s: inteiro
inicio
      cont <- 1
      s <- 0
      enquanto (cont <= 5)faca
      escreva ("digite o", cont, "o.valor : ")
      leia (n)
      cont <- cont + 1
   fimenquanto
              escreval ("a soma de todos os valores foi ", s)
fimalgoritmo
